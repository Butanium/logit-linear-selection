[project]
name = "logit-linear-selection"
version = "0.1.0"
description = "Subliminal Effects in Your Data: A General Mechanism via Log-Linearity (LLS) — runnable fork."
requires-python = ">=3.11,<3.13"
dependencies = [
    "torch<2.10",
    "transformers",
    "datasets",
    "accelerate",
    "trl",
    "peft",
    "numpy",
    "pyyaml",
    "tqdm",
    "inflect",
]
